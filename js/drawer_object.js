window.DrawerObject =
    class DrawerObject {
        constructor(map) {
            this.map = map;
        }

        drawObject() {
            console.log("Draw NoObject!");
        }

        animation(dx, dy) {
            console.log("Animate NoObject!");
        }
    }
;